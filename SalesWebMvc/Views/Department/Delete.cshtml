@model SalesWebMvc.Models.Department

@{
    ViewData["Title"] = "Delete";
}

<h2 class="mt-4">Delete Department</h2>

<div>
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>

    <h3 class="mt-2">Are you sure you want to delete this?</h3>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="DepartmentId" />
        <input type="submit" value="Delete" class="btn btn-warning mt-2 me-2" />
        <a asp-action="Index" class="btn btn-outline-light mt-2">Back to List</a>
    </form>
</div>
