@model SalesWebMvc.Models.Department

@{
    ViewData["Title"] = "Details";
}

<h2 class="mt-4">Department Details</h2>

<div>
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.DepartmentId" class="btn btn-success mt-2">Edit</a>
    <a asp-action="Index" class="btn btn-outline-light mt-2">Back to List</a>
</div>
